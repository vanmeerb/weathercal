[project]
name = "weathercal"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pandas",
    "requests",
    "icalendar",
    "openmeteo-requests",
    "requests-cache",
    "retry-requests",
    "markdown",
]
